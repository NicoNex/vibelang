mod DropLet

;; `s` is owned, is only ever read through a borrow, and nothing takes it: it
;; dies at the end of the body that binds it.
keep (n:U32) : U32 =
  let s = concat "a" "b" in
  n + u32 (len &s)
